---
kind: Service
apiVersion: v1

metadata:
    name: assets-web-v1

spec:
    selector:
        platform: sweetrpg
        app: assets
        component: web
        version: v1
    type: LoadBalancer
    ports:
        - name: http
          port: 8081
          targetPort: http
