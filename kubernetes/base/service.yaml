---
kind: Service
apiVersion: v1

metadata:
    name: assets-web-v1

spec:
    selector:
        app: sweetrpg-assets-web-v1
        platform: sweetrpg
        package: assets
        component: web
        version: v1
    type: LoadBalancer
    ports:
        - name: http
          port: 8081
          targetPort: http
