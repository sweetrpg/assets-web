---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
    name: assets-web-data

spec:
    accessModes:
        - ReadWriteMany
    storageClassName: nfs-client
    resources:
        requests:
            storage: 10Gi
