---
apiVersion: v1
kind: ConfigMap

metadata:
    name: web-env

data:
    PORT: "8081"
    APPLICATION_BASE_PATH: ""
    BUILD_INFO_PATH: /app/build-info.json
    ASSET_DATA_PATH: /data
    SWEETRPG_ROOT_URL: https://dev.sweetrpg.com/
    # Flask SCRIPT_NAME for url_for() to generate correct paths when behind /assets prefix
    SCRIPT_NAME: /assets
    # Requests per minute, one shared process-wide bucket - a blunt backstop, not per-client.
    RATE_LIMIT: "120/minute"
