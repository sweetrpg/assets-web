---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor

metadata:
    name: sweetrpg-assets-web
    labels:
        release: prometheus

spec:
    namespaceSelector:
        matchNames:
            - sweetrpg-assets
    podMetricsEndpoints:
        - port: http
    selector:
        matchLabels:
            app: web
            platform: sweetrpg
            package: assets
            component: web
