---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
    name: assets-web-data

spec:
    storageClassName: standard
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 10Gi
