---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
    name: web-data

spec:
    storageClassName: standard
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 10Gi
